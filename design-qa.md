# 开户与 USDT 结算 MVP 验证

2026-09-15。

- 8 项自动测试通过：币种/网络/精度、到账幂等、结算冻结及完成、过期/余额/取消、状态重校验、完整开户结算闭环与请求字段、材料大小与条件材料、演示身份重置。
- TypeScript/Vite 生产构建通过，git diff --check 通过。
- 浏览器完成：登录 → 未开户引导 → 关联当前邮箱创建企业 → 填入 KYC 演示资料 → 提交审核 → 模拟通道通过 → 提交银行 → 模拟银行通过 → 收款1000 USDT → 可用995 USDT → 兑换900 USDT → 实际模拟到账894.60 USD → 在交易订单查看详情。
- KYC 提交后显示审核中且无资料编辑表单；银行资料仅在通道通过后显示。
- 390×844 视口检查：开户表单单列、字段区宽297px、页面scrollWidth375px，没有横向溢出。缺少资料提交后错误摘要聚焦并滚动可见。
- 美元换 U 及参考入口移除；交易详情区分预计/实际到账，未完成时完成时间为空。

限制：没有真实上传或 API 调用；扩展名/大小校验不替代 MIME/字节内容及服务端归属校验。未测试真实设备、屏幕阅读器、系统字体放大、200%缩放或虚拟键盘。完整银行场景、其他KYC类型及真实审核通知不在本期。

## 多银行与 USD 付款验收

- 企业认证通过后直接显示企业信息，银行管理独立。
- 已通过浏览器验证：第三方企业美国 SWIFT 账户添加、审核、USDT 充值、报价、采购用途确认、模拟付款完成。
- 1000 USDT 示例充值净入账995；付款900，实际到账894.60 USD，余额95，冻结清零。
- 银行及付款详情可在窄屏查看，弹窗内展示账号、关系、用途与完成时间。
- 11项模型测试覆盖多银行关联、六场景、重复账户、审核拦截、活动订单禁止删除、报价和资金幂等。
- 全部为前端模拟，未调用正式接口。
